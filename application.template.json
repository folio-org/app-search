{
  "name": "${project.name}",
  "version": "${project.version}",
  "description": "${project.description}",
  "platform": "complete",
  "dependencies": [
    {
      "name": "app-platform-minimal",
      "version": "^2.1.0",
      "preRelease": "only"
    }
  ],
  "modules": [
    {
      "name": "mod-search",
      "version": "latest",
      "preRelease": "only"
    }
  ]
}
