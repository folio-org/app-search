{
  "name": "${project.name}",
  "version": "${project.version}",
  "description": "${project.description}",
  "platform": "complete",
  "dependencies": [
    {
      "name": "app-platform-minimal",
      "version": "^2.1.0-SNAPSHOT",
      "preRelease": "only"
    }
  ],
  "modules": [
    {
      "name": "mod-search",
      "version": "6.0.0-SNAPSHOT.2064",
      "preRelease": "only"
    }
  ]
}
